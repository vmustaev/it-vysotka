# Управление волонтерами и синхронизация данных

## Обзор

Система управления волонтерами позволяет администратору создавать аккаунты волонтеров, которые могут отмечать присутствие участников на чемпионате. Реализована синхронизация в реальном времени между несколькими волонтерами через WebSocket.

## Возможности

### Для администратора

1. **Создание волонтеров**
   - Админ может создать аккаунт волонтера через админ-панель
   - Указывается email, пароль, имя, фамилия и отчество
   - Волонтеры активируются автоматически

2. **Управление волонтерами**
   - Просмотр списка всех волонтеров
   - Смена пароля волонтера
   - Удаление волонтера

3. **Доступ**
   - Админ-панель → Волонтеры (`/admin/volunteers`)

### Для волонтера

1. **Отметка присутствия**
   - Просмотр всех участников, сгруппированных по кабинетам
   - Отметка отдельного участника как присутствующего/отсутствующего
   - Массовая отметка всех участников кабинета

2. **Фильтрация**
   - Поиск по ФИО участника
   - Фильтр по кабинетам

3. **Экспорт**
   - Экспорт посадочных листов в Excel
   - Автоматическая группировка по командам
   - Только присутствующие участники

4. **Статистика**
   - Общее количество участников
   - Количество присутствующих
   - Процент явки

## Синхронизация в реальном времени

### Технология

Используется **WebSocket** (Socket.IO) для синхронизации данных между несколькими волонтерами.

### Как это работает

1. **Подключение**
   - При входе волонтера в систему автоматически устанавливается WebSocket соединение
   - Аутентификация через JWT токен

2. **События**
   - `attendance:updated` - обновление статуса присутствия участника
   - `statistics:updated` - обновление общей статистики

3. **Автообновление**
   - Когда один волонтер отмечает участника, все остальные волонтеры видят изменения мгновенно
   - Статистика обновляется автоматически для всех волонтеров

### Преимущества

- **Нет конфликтов данных** - все волонтеры работают с актуальными данными
- **Быстрая работа** - изменения видны мгновенно без перезагрузки страницы
- **Масштабируемость** - может работать любое количество волонтеров одновременно

## API Endpoints

### Управление волонтерами (только для админа)

```
POST   /api/admin/volunteers                    - Создать волонтера
GET    /api/admin/volunteers                    - Получить список волонтеров
DELETE /api/admin/volunteers/:volunteerId       - Удалить волонтера
PUT    /api/admin/volunteers/:volunteerId/password - Обновить пароль волонтера
```

### Отметка присутствия (для волонтеров)

```
GET    /api/volunteer/participants              - Получить список участников с рассадкой
POST   /api/volunteer/attendance/mark           - Отметить присутствие участника
POST   /api/volunteer/attendance/mark-multiple  - Массовая отметка присутствия
GET    /api/volunteer/attendance/statistics     - Получить статистику посещаемости
GET    /api/volunteer/attendance/export-all-rooms - Экспорт посадочных листов (Excel)
```

## Структура базы данных

### Таблица Users

Добавлено:
- `role` ENUM('participant', 'admin', 'volunteer') - роль пользователя
- `attendance` BOOLEAN DEFAULT false - отметка о присутствии

## Компоненты

### Backend

1. **websocket.js** - WebSocket сервер
   - Инициализация Socket.IO
   - Аутентификация соединений
   - Управление комнатами
   - Broadcast функции

2. **service/user-service.js**
   - `createVolunteer()` - создание волонтера
   - `getVolunteers()` - получение списка волонтеров
   - `deleteVolunteer()` - удаление волонтера
   - `updateVolunteerPassword()` - обновление пароля

3. **service/attendance-service.js**
   - `getParticipantsWithSeating()` - получение участников с рассадкой
   - `markAttendance()` - отметка присутствия
   - `markMultipleAttendance()` - массовая отметка
   - `getAttendanceStatistics()` - статистика

4. **controllers/user-controller.js** - контроллеры для управления волонтерами
5. **controllers/attendance-controller.js** - контроллеры для отметки присутствия
6. **middlewares/volunteer-middleware.js** - проверка прав волонтера

### Frontend

1. **services/WebSocketService.js** - WebSocket клиент
   - Подключение к серверу
   - Подписка на события
   - Обработка обновлений

2. **services/VolunteerService.js** - API для управления волонтерами
3. **services/AttendanceService.js** - API для отметки присутствия

4. **pages/admin/Volunteers.js** - страница управления волонтерами
5. **pages/volunteer/VolunteerDashboard.js** - дашборд волонтера
6. **pages/volunteer/AttendanceList.js** - страница отметки присутствия

## Использование

### Создание волонтера

1. Войдите как администратор
2. Перейдите в раздел "Волонтеры"
3. Нажмите "Создать волонтера"
4. Заполните форму:
   - Email (для входа)
   - Пароль (минимум 6 символов)
   - Имя
   - Фамилия
   - Отчество (необязательно)
5. Нажмите "Создать"

### Работа волонтера

1. Войдите с учетными данными волонтера
2. Вы попадете на дашборд с общей статистикой
3. Перейдите в "Отметка присутствия"
4. Используйте фильтры для поиска участников
5. Отмечайте присутствие:
   - Кликните на кнопку "Отметить" рядом с участником
   - Или используйте "Отметить всех" для целого кабинета
6. Экспортируйте посадочные листы при необходимости

### Смена пароля волонтера

1. Войдите как администратор
2. Перейдите в раздел "Волонтеры"
3. Найдите нужного волонтера
4. Нажмите кнопку "Пароль"
5. Введите новый пароль дважды
6. Нажмите "Обновить пароль"

## Безопасность

1. **Аутентификация**
   - Все запросы требуют JWT токен
   - WebSocket соединения также аутентифицируются

2. **Авторизация**
   - Middleware проверяет роль пользователя
   - Волонтеры имеют доступ только к своим функциям
   - Админы имеют полный доступ

3. **Валидация**
   - Все входные данные валидируются на сервере
   - Пароли хешируются с использованием bcrypt

## Мониторинг

### Логи WebSocket

Backend выводит в консоль:
- ✅ Подключения пользователей
- 📡 Присоединения к комнатам
- 📤 Отправленные обновления
- ❌ Отключения
- ⚠️ Ошибки

Frontend выводит в консоль браузера:
- 🔄 Полученные обновления посещаемости
- 📊 Обновления статистики

## Устранение неполадок

### WebSocket не подключается

1. Проверьте, что backend запущен
2. Проверьте CORS настройки в `backend/server.js`
3. Проверьте, что порт 4000 доступен
4. Откройте консоль браузера для деталей ошибки

### Данные не синхронизируются

1. Проверьте WebSocket подключение в консоли
2. Убедитесь, что токен валиден
3. Проверьте логи backend на наличие ошибок
4. Перезагрузите страницу

### Не удается создать волонтера

1. Проверьте, что email уникален
2. Убедитесь, что пароль не менее 6 символов
3. Проверьте права доступа (только админ)

## Технические детали

### WebSocket комнаты

- Волонтеры и админы автоматически присоединяются к комнате `volunteers`
- Все обновления отправляются только в эту комнату
- Участники не получают WebSocket обновления

### Оптимизация

- WebSocket подключается только один раз при входе
- Обновления отправляются только при изменении данных
- Локальное состояние обновляется мгновенно для лучшего UX

### Масштабирование

Для работы с большим количеством волонтеров:
1. Используйте Redis adapter для Socket.IO
2. Настройте load balancer с sticky sessions
3. Увеличьте лимиты подключений в nginx

## Будущие улучшения

- [ ] История изменений посещаемости
- [ ] Уведомления о конфликтах (если два волонтера редактируют одновременно)
- [ ] Офлайн режим с синхронизацией при подключении
- [ ] Мобильное приложение для волонтеров
- [ ] QR-коды для быстрой отметки присутствия

