# Файловая Система - Инструкция по установке

## 📋 Что было создано

### Backend:
1. **Модель** - `models/file-model.js` - Sequelize модель для работы с файлами
2. **Миграция** - `migrations/20260128-create-files-table.js` - Создание таблицы files в БД
3. **Сервис** - `service/file-service.js` - Бизнес-логика работы с файлами
4. **Контроллер** - `controllers/file-controller.js` - Обработка HTTP запросов
5. **Роуты** - Добавлены в `router/index.js`
6. **Скрипт миграции** - `scripts/run-migration.js` - Запуск миграций

### Frontend:
1. **Сервис** - `src/services/FileService.js` - API клиент для работы с файлами
2. **Компонент** - `src/pages/admin/FileManager.js` - Админ-панель управления файлами
3. **Стили** - `src/styles/file-manager.css` - Стили для FileManager

## 🚀 Установка

### 1. Запустите миграцию

Из папки `backend` выполните:

```bash
npm run migrate
```

Или напрямую:

```bash
node scripts/run-migration.js
```

Это создаст таблицу `files` в вашей базе данных PostgreSQL.

### 2. Перезапустите backend

Если backend запущен, перезапустите его, чтобы подключились новые модели:

```bash
# В папке backend
npm start
# или если используете docker
docker-compose restart backend
```

### 3. Добавьте роут в админ-панель (Frontend)

Откройте файл с роутами админ-панели (например, `src/pages/admin/AdminDashboard.js` или `src/App.js`) и добавьте:

```javascript
import FileManager from './pages/admin/FileManager';

// В роутах добавьте:
<Route path="/admin/files" element={<FileManager />} />
```

Также добавьте ссылку в меню админ-панели:

```javascript
<Link to="/admin/files">Управление файлами</Link>
```

## 📝 Структура таблицы

**Таблица: files**

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | ID файла (автоинкремент) |
| filename | STRING | Оригинальное имя |
| savedFilename | STRING | Уникальное имя в ФС |
| filepath | STRING | Путь к файлу |
| fileType | ENUM | Тип: gallery, sponsors, certificates, tasks, regulations, results, other |
| mimetype | STRING | MIME тип |
| size | INTEGER | Размер в байтах |
| description | TEXT | Описание (необязательно) |
| isActive | BOOLEAN | Активен ли файл |
| uploadedBy | INTEGER | ID загрузившего пользователя |
| createdAt | DATE | Дата создания |
| updatedAt | DATE | Дата обновления |

## 🔌 API Endpoints

### Admin endpoints (требуется аутентификация + роль admin):

- `POST /api/admin/files/upload` - Загрузить файл
- `GET /api/admin/files` - Получить все файлы (с фильтрами)
- `GET /api/admin/files/stats` - Статистика файлов
- `GET /api/admin/files/:id` - Получить файл по ID
- `PUT /api/admin/files/:id` - Обновить информацию о файле
- `DELETE /api/admin/files/:id` - Удалить файл

### Public endpoints (без аутентификации):

- `GET /api/files/type/:type` - Получить файлы по типу
- `GET /files/:savedFilename` - Прямой доступ к файлу

## 💡 Примеры использования

### Backend - загрузка файла через curl:

```bash
curl -X POST http://localhost:4000/api/admin/files/upload \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@photo.jpg" \
  -F "fileType=gallery" \
  -F "description=Фото с мероприятия"
```

### Backend - получение файлов галереи (public):

```bash
curl http://localhost:4000/api/files/type/gallery
```

### Frontend - загрузка файла:

```javascript
import FileService from '../services/FileService';

// Загрузка файла
const handleUpload = async (file, fileType, description) => {
  try {
    const result = await FileService.uploadFile(file, fileType, description);
    console.log('Файл загружен:', result.file);
  } catch (error) {
    console.error('Ошибка:', error);
  }
};
```

### Frontend - получение файлов по типу:

```javascript
import FileService from '../services/FileService';

// Получить все изображения галереи
const loadGallery = async () => {
  try {
    const result = await FileService.getFilesByType('gallery');
    console.log('Файлы галереи:', result.files);
    
    // Отобразить изображения
    result.files.forEach(file => {
      console.log(`URL изображения: ${file.url}`);
    });
  } catch (error) {
    console.error('Ошибка:', error);
  }
};
```

### Frontend - отображение изображения:

```javascript
// В вашем компоненте
const [galleryFiles, setGalleryFiles] = useState([]);

useEffect(() => {
  const loadGallery = async () => {
    const result = await FileService.getFilesByType('gallery');
    setGalleryFiles(result.files);
  };
  loadGallery();
}, []);

// В JSX
return (
  <div className="gallery">
    {galleryFiles.map(file => (
      <img key={file.id} src={file.url} alt={file.filename} />
    ))}
  </div>
);
```

## 🎯 Типы файлов

- **gallery** - Галерея (фото с мероприятий)
- **sponsors** - Логотипы спонсоров
- **certificates** - Шаблоны сертификатов
- **tasks** - Файлы заданий (PDF, DOCX и т.д.)
- **regulations** - Положения, регламенты
- **results** - Файлы с результатами
- **other** - Прочие файлы

## ⚙️ Конфигурация

Максимальный размер файла настраивается в `router/index.js`:

```javascript
const upload = multer({ 
    storage: multer.memoryStorage(),
    limits: { fileSize: 10 * 1024 * 1024 } // 10MB limit
});
```

## 🔒 Безопасность

- Загрузка файлов доступна только администраторам
- Удаление файлов доступно только администраторам
- Просмотр файлов доступен всем (публичный endpoint)
- Файлы сохраняются с уникальными именами (UUID)
- При удалении записи из БД, физический файл также удаляется

## 📂 Хранение файлов

Все файлы хранятся в папке `backend/files/` и доступны через статический роут `/files/*`.

Пример:
- Загружен файл `photo.jpg`
- Сохранен как `a1b2c3d4-e5f6-7890-abcd-ef1234567890.jpg`
- Доступен по URL: `http://localhost:4000/files/a1b2c3d4-e5f6-7890-abcd-ef1234567890.jpg`

## 🐛 Отладка

Если что-то не работает:

1. Проверьте, что миграция выполнена успешно
2. Убедитесь, что backend перезапущен после добавления новых файлов
3. Проверьте права доступа к папке `backend/files/`
4. Убедитесь, что в `.env` корректные настройки БД
5. Проверьте логи backend в консоли

## 📚 Дополнительная документация

Полная документация API: `FILE_SYSTEM_API.md`
